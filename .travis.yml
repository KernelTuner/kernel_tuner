language: python
python:
  - "2.7"
# command to install dependencies
install:
  - pip install mock
# command to run tests
script: nosetests -v
branches:
  only:
    - master
notifications:
  email: false
